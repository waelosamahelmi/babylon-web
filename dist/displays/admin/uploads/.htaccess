# Allow only images
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny everything else
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
